## What changed

Describe the user-visible behavior and why it changed.

## Safety impact

Describe changes to credentials, commands, filesystem access, tracker writes, network access, or agent autonomy. Write "None" when these boundaries are unchanged.

## Validation

- [ ] `python -m unittest discover -s tests -v`
- [ ] `python -m compileall -q src tests`
- [ ] Package build succeeds
- [ ] Documentation reflects configuration or behavior changes

